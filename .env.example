# Django Environment Configuration
# Copy this file to .env and customize as needed

# Debug mode (1 = enabled, 0 = disabled)
# IMPORTANT: Set to 1 for development, 0 for production
DJANGO_DEBUG=1

# Secret key for cryptographic signing
# Leave empty in development - will auto-generate with warning
# For production, generate one with:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=

# Allowed hosts (comma-separated)
# Default: localhost,127.0.0.1
# For production, set to your domain: example.com,www.example.com
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database filename
# Default: db.sqlite3
DJANGO_DB_NAME=db.sqlite3

# ===== Email Encryption (MCO 1) =====
# 32-byte AES-256 encryption key for email addresses (base64 encoded)
#
# CRITICAL SECURITY NOTES:
# 1. Generate a NEW key for each environment (dev, staging, prod)
# 2. NEVER commit this key to git
# 3. Back up this key securely - if lost, emails cannot be decrypted!
# 4. Rotate keys periodically (requires re-encryption)
#
# To generate a secure key:
#   python -c "from accounts.encryption import generate_encryption_key; print(generate_encryption_key())"
#
# Example (DO NOT USE THIS - generate your own):
#   ACCOUNT_EMAIL_ENCRYPTION_KEY=dGhpc2lzYXRlc3RrZXkxMjM0NTY3ODkwMTIzNDU2Nzg5MDEy
#
# If not set, a default key will be derived from SECRET_KEY (not recommended for production)
ACCOUNT_EMAIL_ENCRYPTION_KEY=
